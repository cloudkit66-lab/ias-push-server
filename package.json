{
  "name": "nasir-ias-push-server",
  "version": "1.0.0",
  "description": "Tiny backend that sends real Web Push notifications for the Nasir IAS Tracker, even when the phone's browser is fully closed.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "web-push": "^3.6.7",
    "express": "^4.19.2",
    "cors": "^2.8.5"
  }
}
